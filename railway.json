{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS", "buildCommand": "pip install -U -r requirements.txt" },
  "deploy": {
    "startCommand": "python3 -u bot.py",
    "restartPolicyType": "ALWAYS",
    "restartPolicyMaxRetries": 1000000
  }
}
