# اتصال دائمی گیت‌هاب — دو راه

## راه ۱ (پیشنهادی - یک بار برای همیشه)
توکن PAT گیت‌هاب را برای دستیار بفرست (تنظیمات گیت‌هاب ← Developer settings ← Personal access tokens ← Generate - دسترسی repo).
دستیار آن را فقط در `.env` ذخیره می‌کند (هرگز داخل ریپو/کد نمی‌رود) و از آن پس:
- هر نسخهٔ جدید به‌صورت دائمی push می‌شود
- با Revoke کردن توکن قدیمی، نسخهٔ قدیمی ابری (RE) هم برای همیشه خاموش می‌شود

## راه ۲ (بدون توکن)
گیت‌هاب ← ریپو ← تب Actions ← کاری «iran-bot» ← ⋯ ← **Disable workflow**
(نسخهٔ قدیمی ابری خاموش می‌شود؛ اجرای دائمی نسخهٔ جدید سرور فعلی است)

## بستهٔ پشتیبان
`iranguard-deploy.bundle` = کل تاریخچهٔ پروژه در یک فایل؛ هر جا قابل بازکردن است:
```
git clone iranguard-deploy.bundle -b master iranguard
```


## اجرای سرور (نگهبان)
```
bash run.sh        # اگر بات به هر دلیل افتاد، ۳ ثانیه بعد خودش بالا می‌آید
tail -f logs/bot.log   # لاگ زنده
```
- خروج نرمال: سیگنال TERM/INT → بکاپ exit + ذخیره کامل.
- بکاپ خودکار هر ۳۰ دقیقه در data/backup (۱۲ نسخه آخر نگه داشته می‌شود).
- نسخهٔ فعلی: v6.33 (کامیت 46be047 + سخت‌سازی دیپلوی).
- لاگ بدون بافر (`python3 -u`) - پرینت‌ها همان لحظه در logs/bot.log می‌افتند.


## حالت فعال (۲۰۲۶-۰۹-۱۲): هاست ۲۴/۷ روی GitHub Actions (اکانت دوم)
- ریپو: khwnm511-commits/iranguard-bot (public) — workflow `bot.yml`
- زنجیره: ران ~۵h50m → commit data → dispatch ران بعد (تا سقف ۶h؛ + کرون «35 */6 * * *»)
- سکرت‌ها: BOT_TOKEN، DEPLOY_PAT — data هر دوره commit/push می‌شود
- Dockerfile اکنون data/ را هم bake می‌کند (اسنپ‌شات)؛ `.dockerignore` بک‌آپ/لاگ را حذف می‌کند
- Railway: سرویس به همین ریپو اشاره می‌کند ولی deploy نیازمند lift محدودیت workspace (کارت) — خوابیده
- ⚠️ حین ران فعالِ رانر، data از سندباکس push نشود (ری‌بیسِ save رانر را می‌شکند)
